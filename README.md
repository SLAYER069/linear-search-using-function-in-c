A linear search in C can be implemented using a function to make the code modular and reusable. This function searches for a target element within an array and returns its index if found, or a specific value (commonly -1) if not found. 
Here is an example of a linear search function in C
